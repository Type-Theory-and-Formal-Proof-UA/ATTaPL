# Правила перекладу (ATTaPL → українська)

Читай цей файл повністю перед початком. Він обов’язковий.

## 1. Що перекладаємо

Повний переклад, не переказ. Кожне означення, доведення, приклад, зауваження, вправа,
підпис рисунка, зноска й посилання переноситься. Якщо в доведенні загубився крок — це
дефект, а не стилістичний вибір. Нічого не скорочуй.

**Що НЕ перекладається (лишається як в оригіналі):**
- назви правил типізації: `T-App`, `E-IfTrue`, `M-Lin1`, `Q-Trans`, `CTX-Empty`, `K-App`;
- назви мов і систем: `TAL-0`, `TAL-1`, `JVML`, `LF`, `HM(X)`, `ML`, `Standard ML`,
  `Objective Caml`, `Haskell`, `System F`;
- імена авторів у бібліографії та заголовках: `David Walker`, `Greg Morrisett`, … ;
- ключові слова мов програмування в коді (`type`, `val`, `let`, `if`, `then`, `else`,
  `fun`, `struct`, `sig`, `int`, `bool`, `unit`, `true`, `false`);
- літерні символи, які навмисно використовуються у формулах (`lin`, `un`, `ord`, `aff`, `rel`).

Усе інше — включно з пояснювальним текстом усередині рисунків і таблиць — перекладай.

## 2. Структура файлу

Файл, який ти пишеш, — це Typst. Він мусить починатися так:

```typst
#import "/templates/preamble.typ": *

#chap("1", "Substructural Type Systems")[David Walker]
```

`chap` приймає рівно три позиційні аргументи: номер глави (рядок, `""` для частин без
номера), назву (перекладена) та автора (латиницею, як в оригіналі).

Далі — тіло. Заголовки розділів і підрозділів:

- `#sec("1.1", "Структурні властивості")` — розділ (у дужках номер і перекладена назва);
- `#subsec("Синтаксис")` — підрозділ;
- `#subsubsec("…")` — підпідрозділ.

Номер розділу (`1.1`) залишається **латиницею-цифрами як в оригіналі**, назва — перекладена.
`#sec(...)` скидає лічильник тверджень, тому кожен розділ нумерується з одиниці —
саме так, як у книзі.

## 3. Середовища тверджень

Ніколи не пиши номер твердження руками — його друкує середовище:

| Оригінал | Українською |
|---|---|
| Theorem | `#thm[…]` |
| Lemma | `#lem[…]` |
| Corollary | `#cor[…]` |
| Definition | `#defn[…]` |
| Proposition | `#prop_(…)` |
| Claim | `#claim[…]` |
| Notation | `#notation[…]` |
| Convention | `#convention[…]` |
| Fact | `#fact[…]` |
| Axiom | `#axiom[…]` |
| Example | `#example[…]` |
| Remark | `#remark[…]` |
| Figure | `#figure([Figure 1-1: …], body)` |
| Exercise | `#exr[… ]` |

Приклади:

```typst
#lem[Якщо $Gamma_1, x_1 : T_1, Gamma_2 tack t : T$, то $Gamma_1, x_2 : T_2, Gamma_2 tack t : T$.]
#defn[Терм $t$ є #emph{значенням}, якщо він не має кроків зведення.]
#exr(diff: "«", rec: true)[Доведіть лему про послаблення.]
#exr(diff: "«««", sol: true)[Покажіть, що типізація розв’язна.]
```

`diff` — це рівень складності з оригіналу (`«`, `««`, `«««`, `««««`), `rec: true` —
позначка `[Recommended]`, `sol: true` — позначка `3` (є розв’язок у додатку A).
Оригінал позначає вправи як `Exercise [«, 3]` — порядок саме такий: спершу `Recommended`,
потім рівень, потім `✓`. Не додавай `✓`, якщо в оригіналі його немає.

**Нумерація.** Номер твердження складається як `розділ.№` (наприклад `Lemma 1.2.3`). Це
збігається з оригіналом: у книзі друга лема розділу 1.2 має номер 1.2.2. Тому **розділи мають
бути заведені в тому ж порядку, що й в оригіналі**, і в кожному розділі не можна
пропускати чи додавати середовища — інакше всі номери після пропуску поїдуть.

Якщо в оригіналі твердження **не має** номера (просте речення, не виділене середовищем),
не загортай його в середовище. Якщо середовище цитує чуже твердження з оригінальним
номером — надрукуй номер текстом, а не середовищем.

## 4. Математика

Математика в Typst — у доларах: `$Gamma tack t : T$`. Текст усередині математики
пишеться в лапках: `$T ::= "Bool" mid T times T$`.

**Перевірена таблиця символів** (перевірена компіляцією та читанням готового PDF —
користуйся тільки цими написаннями):

| Потрібен символ | Пиши |
|---|---|
| ⊢ | `tack` |
| → | `arrow.r` |
| ⇒ | `arrow.r.double` |
| ⇔ | `arrow.l.r.double` |
| ⟺ | `arrow.l.r.double.long` |
| ↦ | `arrow.r.bar` |
| ↑ / ↓ | `arrow.t` / `arrow.b` |
| ⪯ | `prec.eq` |
| ≼ | `prec.curly.eq` |
| ⊑ | `subset.eq.sq` |
| ⊆ / ⊇ | `subset.eq` / `supset.eq` |
| ∣ (ділить) | `divides` |
| ∈ / ∉ | `in` / `in.not` |
| ∅ | `emptyset` |
| ∀ / ∃ | `forall` / `exists` |
| ∧ / ∨ / ¬ | `and` / `or` / `not` |
| ≠ / ≤ / ≥ | `eq.not` / `lt.eq` / `gt.eq` |
| ⊥ / ⊤ | `bot` / `top` |
| ℓ | `ell` |
| ∂ | `partial` |
| ∇ | `nabla` |
| ≔ | `colon.eq` |
| ≡ | `equiv` |
| ≈ | `approx` |
| ∼ / ≃ | `tilde` / `tilde.eq` |
| ∘ | `compose` |
| ⋅ | `dot.op` |
| ⋆ | `star` |
| ∥ | `parallel` |
| ⊧ / ⊩ | `models` / `forces` |
| ▶ | `triangle.r` |
| □ | `square` |
| ⟨ ⟩ | `chevron.l` `chevron.r` |
| ∞ | `infinity` |
| × | `times` |
| λ, Γ, Δ, Π, Σ, Ψ, Λ, Ω | `lambda`, `Gamma`, `Delta`, `Pi`, `Sigma`, `Psi`, `Lambda`, `Omega` |
| α β γ δ ϵ ε ζ η θ κ μ ν ξ ρ σ τ φ ϕ χ ψ ω | `alpha` `beta` `gamma` `delta` `epsilon.alt` `epsilon` `zeta` `eta` `theta` `kappa` `mu` `nu` `xi` `rho` `sigma` `tau` `phi` `phi.alt` `chi` `psi` `omega` |

**Увага — дві пастки, на яких уже горіли:**
1. `arrow.r.double.long` друкує **⟹**, а не ⇒. Для ⇒ завжди `arrow.r.double`.
2. `arrow.r.long.double` друкує те саме ⟹. Не користуйся ним.
3. Немає символів `otimes`, `oplus`, `angle.l`, `angle.r`, `arrow.up`, `arrow.down`,
   `times.circle`, `preceq`, `turnstile`. Якщо вони трапляються в оригіналі — вставляй
   сам символ Unicode у математику: `$⊗$`, `$⋄$`, `$▶$`, `$⪯$`. Це перевірено й працює.

Індекси: `$T_1$`, `$x_i$`, `$Gamma_1, Gamma_2$`. Пробіли в математиці значущі: пиши
`$Gamma tack t : T$`, а не `$Gammatackt:T$`.

## 5. Рисунки, правила виведення, таблиці

Оригінал містить багато рисунків із правилами виведення та боксами синтаксису. Вони
переносяться так:

```typst
#figure([Figure 1-1: Simply-typed lambda calculus with booleans], rules([
  Syntax   #h(1em) $Gamma tack t : T$
  ...
  #rule(
    $Gamma tack t_1 : T_11 -> T_12 quad Gamma tack t_2 : T_11$,
    "T-App",
    $Gamma tack t_1 t_2 : T_12$,
  )
]))
```

`rules(body: …)` малює рамку й зберігає розбиття на рядки, як надруковано.
`rule(посилки, назва, висновок)` малює посилки над рискою з назвою праворуч і висновок
під нею. Назву правила (`T-App`) подавай рядком у лапках.

Усередині `rules[…]` не можна користуватися `#h(...)`, `#v(...)`, `#grid(...)`; текст
просто пиши рядками, а `#rule(...)` став окремим рядком. Знак ⊢ у правилах уже
надруковано як `$Gamma tack t : T$` — у готовому PDF він виглядає як «Γ ⊢ t : T»;
не додавай `tack` окремо перед `$Gamma$`.

Якщо з верстки незрозуміло, як розкласти рисунок, відтвори зміст рядок за рядком —
важливо не загубити жодного правила. **Не вигадуй геометрію й не витрачай виклики на
вимірювання координат: розбиття на рядки вже є в тексті джерела.**

Підпис рисунка перекладай, але номер лишай як в оригіналі: `Figure 1-1:` → `Рисунок 1-1:`.

## 6. Мова

- Апостроф — **тільки `’` (U+2019)**: об’єкт, зв’язування, підстановка, ім’я. Не `'`
  і не `ʼ`.
- Терміни — з `/Users/ihor/atapl/GLOSSARY.md`, без варіантів. Термін, якого там немає,
  бери найуживаніший і **повідом у звіті** (поле `new_terms`), а не вигадуй свій.
- Лапки: «…» для цитат і назв, “…” лише якщо так у оригіналі всередині.
- Тире: — (U+2014) у тексті.
- Позначка ділення ∣ у математиці — `divides` (а не `mid`, який друкує слово).
- Курсив — `#emph[…]` із **квадратними** дужками; `#emph{…}` надрукує `emph{…}` буквально.
  Так само `#strong[…]`, а не `#strong{…}`.
- Не калькуй російські звороти: не «являється» (правильно «є»), не «відповідно до»
  замість «згідно з», не «на протязі» (правильно «протягом»), не «слідуючий»
  (правильно «наступний»), не «існуючий» у значенні «наявний» — але «наявний» теж
  уживай доречно. Пиши живою фаховою українською, як пишуть підручники з
  теоретичної інформатики.
- Англіцизми на кшталт «лема», «теорема», «інстанціювання» (правильно «примірник») —
  уникай кальок, де є нормальний український відповідник.

## 7. Що заборонено

- Редагувати `templates/preamble.typ`, `GLOSSARY.md`, `manifest.json` або файли інших
  частин. Їх має лише батьківський агент. Нові терміни — у звіті, не в глосарії.
- Писати в теку перекладу щось, крім свого файлу `uk.typ`. Пробні файли — в `/tmp`.
- Лишати англійський текст у прозі (крім переліченого в п.1).
- Лишати маркер `TODO`, коли текст уже перекладено. І **прибирати** `TODO`, коли
  розділ справді перекладено: наявність `TODO` — це сигнал, що робота не завершена.
- Скорочувати, переказувати, «залишати на потім».

## 8. Обов’язкова перевірка перед звітом

```bash
cd /Users/ihor/atapl
node_modules/.bin/typst compile --root . out/<частина>/uk.typ /tmp/<частина>.pdf
```

(у цьому середовищі `typst` є в `/opt/homebrew/bin/typst`; якщо `node_modules/.bin/typst`
немає, виклич просто `typst compile --root . out/<частина>/uk.typ /tmp/<частина>.pdf`).

Далі **прочитай готовий PDF**, а не лише код виходу:

```bash
pdftotext -layout /tmp/<частина>.pdf - | head -60
```

Переконайся, що символи друкуються правильно (⇒, а не ⟹; ⪯; ⊢; ⋮), що кирилиця на місці
й що немає порожніх боків. Файл, який не компілюється, вважається неперекладеним.

Пиши файл **у міру готовності**: переклав розділ — зберіг — скомпілював. Не накопичуй
усі правки на один фінальний запис: якщо сесія обірветься, незаписане втрачено повністю.

## 9. Звіт

Поверни JSON:
```json
{
  "file": "out/ch01p01/uk.typ",
  "sections_translated": ["1.1 Структурні властивості", "1.2 Лінійна система типів"],
  "compiled": true,
  "words": 15014,
  "new_terms": [{"en": "thunk", "uk": "затримане обчислення"}],
  "notes": "що лишилось незрозумілим або потребує рішення"
}
```
`compiled` — тільки якщо `typst compile` справді пройшов успішно на твоєму файлі.
